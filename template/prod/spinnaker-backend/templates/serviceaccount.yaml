{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "spinnaker-backend.serviceAccountName" . }}
  namespace: ${ parameters.namespace }
  prodels:
    {{- include "spinnaker-backend.prodels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
